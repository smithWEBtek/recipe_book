<h2>All Comments</h2>
<% @comments.each do |comment| %>
  <div>
    <h4><%= comment.title %></h4>
    <h5>by: <%= link_to comment.user.name, user_path(comment.user) %></h5>
    <p><%= comment.content %></p>
  </div>
<% end %>
